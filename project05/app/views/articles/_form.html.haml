= form_for @article do |f|
  - if@article.errors.any?
    #error_explanation
      %h2 #{ pluralize @article.errors.ocunt, "error" } prohibited this article from being saved:
      %ul
        - @article.errors.full_messages.each do |msg|
          %li= msg

  #field
    = f.label :title
    = f.text_field :title
  #field
    = f.label :author_name
    = f.text_field :author_name
  #field
    = f.label :body
    = f.text_area :body
  #actions
    = f.submit submit_button
